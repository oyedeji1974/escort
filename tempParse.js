const fs = require(" fs\); const html = fs.readFileSync(\public/admin-panel.html\,\utf8\); const start = html.indexOf(\<script>\); const end = html.lastIndexOf(\</script>\); if (start===-1 ; end===-1) { console.error(\script tag not found\); process.exit(1);} const js = html.slice(start+8,end); try { new Function(js); console.log(\JS parse OK\); } catch (e) { console.error(e.stack); process.exit(1); }
